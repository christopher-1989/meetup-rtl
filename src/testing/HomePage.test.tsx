import { describe, test, expect, vi } from 'vitest'
import { screen } from '@testing-library/react'

import '@testing-library/jest-dom/vitest'
import SignUpForm from '../components/SignUpForm/SignUpForm'
import { render } from './test-utils'

describe('Home Page Personalisation', () => {
  test('No user has standard greeting', () => {})

  test('Successfully submitting the Sign Up form displays personalised message', () => {})

  test('A failing request to the Sign Up form does not show personalised message', () => {})
})
